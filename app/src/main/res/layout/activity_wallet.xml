<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
    >

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/status_color"
    tools:context=".Fragment.Wallet">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/toolbar_color"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Dark"/>


    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_marginTop="50dp"
        android:layout_below="@id/toolbar"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_wallet1"
        />

    <TextView
        android:id="@+id/text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Total Balance"
        android:textColor="@color/cardview_light_background"
        android:textStyle="normal"
        android:textSize="25dp"
        android:fontFamily="@font/montserrat"
        android:gravity="center"
        android:layout_marginTop="@dimen/margin_10"
        android:layout_below="@id/image"/>
    <TextView
        android:id="@+id/bal_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="₹ 0.00"
        android:textColor="@color/cardview_light_background"
        android:textStyle="bold"
        android:textSize="20dp"
        android:fontFamily="@font/montserrat"
        android:gravity="center"
        android:layout_margin="@dimen/margin_10"
        android:layout_below="@id/text"/>

    <!--<me.rishabhkhanna.customtogglebutton.CustomToggleButton
        android:id="@+id/add_money"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textOn="Add Money"
        android:textOff="Add Money"
        android:textSize="15sp"
        android:textStyle="normal"
        android:textColor="@color/white"
        android:textAllCaps="false"
        app:uncheckedColor="@color/status_color"
        app:borderWidth="10dp"
        app:checkedBorderColor="@color/white"
        app:checkedColor="#f32"
        app:uncheckedBorderColor="#000"
        app:checkedTextColor="#fff"
        app:uncheckedTextColor="#000"
        app:radius="10dp"
        android:fontFamily="@font/montserrat"
        android:layout_below="@id/bal_text"
        android:layout_marginTop="50dp"
        />-->
</RelativeLayout>

</layout>